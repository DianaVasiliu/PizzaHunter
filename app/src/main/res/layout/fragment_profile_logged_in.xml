<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ProfileLoggedInFragment">

        <ImageView
            android:id="@+id/profile_image"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:layout_marginStart="30dp"
            android:src="@mipmap/ic_profile_placeholder"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:contentDescription="@string/profile_image" />

        <TextView
            android:id="@+id/greeting_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:maxLines="3"
            android:ellipsize="end"
            android:text="@string/greeting"
            android:textSize="25sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@+id/profile_image"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/profile_image"
            app:layout_constraintTop_toTopOf="@+id/profile_image" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:orientation="vertical"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/profile_image">

            <include
                android:id="@+id/edit_profile_button"
                layout="@layout/info_button"
                app:infoButtonText="@{@string/profile_button_1}" />

            <include
                android:id="@+id/favourites_button"
                layout="@layout/info_button"
                app:infoButtonText="@{@string/profile_button_2}" />

            <include
                android:id="@+id/myorders_button"
                layout="@layout/info_button"
                app:infoButtonText="@{@string/profile_button_3}" />

            <Space
                android:layout_width="match_parent"
                android:layout_height="100dp" />

            <include
                android:id="@+id/logout_button"
                layout="@layout/info_button"
                app:infoButtonText="@{@string/profile_button_4}" />
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>